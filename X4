--[[
	c00lSaken - UI แนวนอน + แท็บหมวดหมู่ + ภาษาไทย
	แก้ไขจากสคริปต้นฉบับ: UI เป็นแนวนอน, เลือกหมวดด้วยปุ่มแท็บ, ข้อความเป็นไทย
]]

local Players = game:GetService("Players")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local Lighting = game:GetService("Lighting")
local UIS = game:GetService("UserInputService")
local RunService = game:GetService("RunService")
local Workspace = game:GetService("Workspace")
local TweenService = game:GetService("TweenService")
local HttpService = game:GetService("HttpService")
local Debris = game:GetService("Debris")
local StarterGui = game:GetService("StarterGui")
local LP = Players.LocalPlayer

local Sprinting = ReplicatedStorage:WaitForChild("Systems"):WaitForChild("Character"):WaitForChild("Game"):WaitForChild("Sprinting")
local stamina = require(Sprinting)
stamina.MaxStamina = 100
stamina.MinStamina = -20
stamina.StaminaGain = 100
stamina.StaminaLoss = 5
stamina.SprintSpeed = 28.6
stamina.StaminaLossDisabled = true

local CONFIG_FILE = "c00lsaken_config.json"

local MapFolder = nil
local mapObj = Workspace:FindFirstChild("Map")
if mapObj then MapFolder = mapObj:FindFirstChild("Ingame") end

local DummyNames = {
	"PizzaDeliveryRig", "Mafiaso1", "Mafiaso2", "Builderman", "Elliot",
	"ShedletskyCORRUPT", "ChancecORRUPT", "ChanceCORRUPT", "Mafia1", "Mafia2",
	"Mafia3", "Mafia4", "Mafia5", "Mafia6", "Mafia7", "Mafia8", "Mafia9",
	"GreenGuy", "RedGuy", "BlueGuy", "PurpleGuy", "PinkGuy", "YellowGuy",
	"OrangeGuy", "GreyGuy"
}

local ESPSettings = {
	killerESP = false,
	playerESP = false,
	generatorESP = false,
	itemESP = false,
	pizzaEsp = false,
	pizzaDeliveryEsp = false,
	zombieEsp = false,
	taphTripwireEsp = false,
	tripMineEsp = false,
	twoTimeRespawnEsp = false,
	graffitiEsp = false,
	azureGroundBulbESP = false,
	azureVineESP = false,
	killerColor = Color3.fromRGB(255, 100, 100),
	survivorColor = Color3.fromRGB(100, 255, 100),
	generatorColor = Color3.fromRGB(255, 255, 0),
	itemColor = Color3.fromRGB(200, 200, 0),
	pizzaColor = Color3.fromRGB(200, 150, 0),
	pizzaDeliveryColor = Color3.fromRGB(200, 100, 100),
	zombieColor = Color3.fromRGB(200, 100, 100),
	taphTripwireColor = Color3.fromRGB(100, 0, 100),
	tripMineColor = Color3.fromRGB(255, 0, 255),
	twoTimeRespawnColor = Color3.fromRGB(0, 150, 200),
	graffitiColor = Color3.fromRGB(255, 255, 255),
	azureGroundBulbColor = Color3.fromRGB(0, 255, 200),
	azureVineColor = Color3.fromRGB(200, 0, 255),
	killerFillTransparency = 0.7,
	killerOutlineTransparency = 0.3,
	survivorFillTransparency = 0.7,
	survivorOutlineTransparency = 0.3,
}

local stabEnabled = false
local stabRange = 8
local stabMode = "Behind"
local BACKSTAB_DISTANCE = 1.8
local BACKSTAB_HOLD_TIME = 0.35
local BACKSTAB_COOLDOWN = 10
local lastBackstabTime = 0

local Slash = {
	Enabled = false,
	Range = 8,
	Distance = 2.5,
	HoldTime = 0.5,
	Cooldown = 10,
	Delay = 0.15,
	LastTime = 0,
	Connection = nil,
	Busy = false,
}

local HitboxExt = {
	Enabled = false,
	Size = 1.3,
	Duration = 500,
	Module = nil,
}

local function getHitboxModule()
	if HitboxExt.Module then return HitboxExt.Module end
	local ok, mod = pcall(function()
		return loadstring(game:HttpGet("https://raw.githubusercontent.com/FoxIPro5965/c00lgui/main/Hitbox.lua"))()
	end)
	if ok and mod then
		HitboxExt.Module = mod
		return mod
	end
	-- fallback inline module
	local hit = {}
	local env = (getgenv and getgenv()) or _G
	if env.emergency_stop == nil then
		env.emergency_stop = false
	end
	local function StudsIntoPower(studs)
		return studs * 1.2
	end
	function hit:ExtendHitbox(studs, time)
		local distance = StudsIntoPower(studs)
		local start = tick()
		if env.emergency_stop == true then
			env.emergency_stop = false
		end
		repeat
			game:GetService("RunService").Heartbeat:Wait()
			local char = LP.Character
			while not (char and char.Parent and char:FindFirstChild("HumanoidRootPart") and char.HumanoidRootPart.Parent) do
				game:GetService("RunService").Heartbeat:Wait()
				char = LP.Character
			end
			local hrp = char.HumanoidRootPart
			local velocity = hrp.Velocity
			hrp.Velocity = velocity * distance + (hrp.CFrame.LookVector * distance)
			game:GetService("RunService").RenderStepped:Wait()
			if char and char.Parent and hrp and hrp.Parent then
				hrp.Velocity = velocity
			end
		until tick() - start > tonumber(time) or env.emergency_stop == true
		if env.emergency_stop == true then
			env.emergency_stop = false
		end
	end
	function hit:StopExtendingHitbox()
		env.emergency_stop = true
	end
	HitboxExt.Module = hit
	return hit
end

local function setHitboxExt(state)
	HitboxExt.Enabled = state
	if state then
		local mod = getHitboxModule()
		task.spawn(function()
			mod:ExtendHitbox(HitboxExt.Size, HitboxExt.Duration)
		end)
	else
		if HitboxExt.Module then
			HitboxExt.Module:StopExtendingHitbox()
		end
	end
end

local antiStun = false
local antiSlow = false
local antiBlindness = true
local antiSubspace = true
local antiHiddenStats = false
local toggleState = false
local originalValues = {}

local killerNames = {
	"Slasher", "Jason", "c00lkidd", "JohnDoe", "1x1x1x1", "Noli", "Nosferatu", "Sixer", "Azure",
	"Guest 666", "Jane Doe", "Valkyrie", "Corrupted", "Jeff The Killer"
}

local voidrushcontrol = false
local ORIGINAL_DASH_SPEED = 55
local isOverrideActive = false
local voidrushConnection

local function startOverride()
	if isOverrideActive then return end
	isOverrideActive = true
	voidrushConnection = RunService.RenderStepped:Connect(function()
		local humanoid = LP.Character and LP.Character:FindFirstChildOfClass("Humanoid")
		local rootPart = LP.Character and LP.Character:FindFirstChild("HumanoidRootPart")
		if not humanoid or not rootPart then return end
		humanoid.WalkSpeed = ORIGINAL_DASH_SPEED
		humanoid.AutoRotate = false
		local direction = rootPart.CFrame.LookVector
		local horizontal = Vector3.new(direction.X, 0, direction.Z)
		if horizontal.Magnitude > 0 then
			humanoid:Move(horizontal.Unit)
		end
	end)
end

local function stopOverride()
	if not isOverrideActive then return end
	isOverrideActive = false
	local humanoid = LP.Character and LP.Character:FindFirstChildOfClass("Humanoid")
	if humanoid then
		humanoid.WalkSpeed = 18
		humanoid.AutoRotate = true
		humanoid:Move(Vector3.new(0, 0, 0))
	end
	if voidrushConnection then
		voidrushConnection:Disconnect()
		voidrushConnection = nil
	end
end

RunService.RenderStepped:Connect(function()
	if not voidrushcontrol then return end
	local char = LP.Character
	local voidRushState = char and char:GetAttribute("VoidRushState")
	if voidRushState == "Dashing" then
		startOverride()
	else
		stopOverride()
	end
end)

local autoGen = true
local autoGenSpeed = 0.08
local flow = { nodeDelay = 0.01 }
local function flowKey(n) return n.row .. "-" .. n.col end
local function flowNeighbour(r1, c1, r2, c2)
	if r2 == r1 - 1 and c2 == c1 then return "up" end
	if r2 == r1 + 1 and c2 == c1 then return "down" end
	if r2 == r1 and c2 == c1 - 1 then return "left" end
	if r2 == r1 and c2 == c1 + 1 then return "right" end
	return false
end
local function flowOrder(path, endpoints)
	if not path or #path == 0 then return path end
	local lookup = {}
	for _, n in ipairs(path) do lookup[flowKey(n)] = n end
	local start
	for _, ep in ipairs(endpoints or {}) do
		for _, n in ipairs(path) do
			if n.row == ep.row and n.col == ep.col then
				start = { row = ep.row, col = ep.col }
				break
			end
		end
		if start then break end
	end
	if not start then
		for _, n in ipairs(path) do
			local nb = 0
			for _, d in ipairs({ { -1, 0 }, { 1, 0 }, { 0, -1 }, { 0, 1 } }) do
				if lookup[(n.row + d[1]) .. "-" .. (n.col + d[2])] then nb = nb + 1 end
			end
			if nb == 1 then start = { row = n.row, col = n.col } break end
		end
	end
	if not start then start = { row = path[1].row, col = path[1].col } end
	local pool, ordered = {}, {}
	for _, n in ipairs(path) do pool[flowKey(n)] = { row = n.row, col = n.col } end
	local cur = start
	table.insert(ordered, { row = cur.row, col = cur.col })
	pool[flowKey(cur)] = nil
	while next(pool) do
		local moved = false
		for k, node in pairs(pool) do
			if flowNeighbour(cur.row, cur.col, node.row, node.col) then
				table.insert(ordered, { row = node.row, col = node.col })
				pool[k] = nil
				cur = node
				moved = true
				break
			end
		end
		if not moved then break end
	end
	return ordered
end
local function flowSolve(puzzle)
	pcall(function()
		if not puzzle or not puzzle.Solution then return end
		local indices = {}
		for i = 1, #puzzle.Solution do indices[i] = i end
		for i = #indices, 2, -1 do
			local j = math.random(1, i)
			indices[i], indices[j] = indices[j], indices[i]
		end
		for _, ci in ipairs(indices) do
			local solution = puzzle.Solution[ci]
			if not solution then continue end
			local ordered = flowOrder(solution, puzzle.targetPairs[ci])
			if not ordered or #ordered == 0 then continue end
			puzzle.paths[ci] = {}
			for _, node in ipairs(ordered) do
				table.insert(puzzle.paths[ci], { row = node.row, col = node.col })
				puzzle:updateGui()
				task.wait(autoGen and autoGenSpeed or flow.nodeDelay)
			end
			task.wait(0)
			puzzle:checkForWin()
		end
	end)
end

local currentPuzzle = nil
pcall(function()
	local modFolder = ReplicatedStorage:FindFirstChild("Modules")
	local miniFolder = modFolder and modFolder:FindFirstChild("Minigames")
	local fgFolder = miniFolder and miniFolder:FindFirstChild("FlowGameManager")
	local fgModule = fgFolder and fgFolder:FindFirstChild("FlowGame")
	if fgModule then
		local ok, FG = pcall(require, fgModule)
		if ok and FG and FG.new then
			local orig = FG.new
			FG.new = function(...)
				local p = orig(...)
				currentPuzzle = p
				if autoGen then
					task.spawn(function()
						task.wait(0.05)
						flowSolve(p)
					end)
				end
				return p
			end
		end
	end
end)

local function SolveNow()
	if autoGen then return end
	if currentPuzzle then
		flowSolve(currentPuzzle)
	else
		for _, v in pairs(getgc(true)) do
			if typeof(v) == "table" and rawget(v, "Solution") and rawget(v, "paths") and rawget(v, "updateGui") then
				flowSolve(v)
				break
			end
		end
	end
end

local AimPunch = {
	Distance = 100,
	Duration = 0.6,
	Mode = "CamChar", -- "CamChar" | "Character"
	Connection = nil,
	Aiming = false,
}
local triggerModules = {}

local function registerTriggerModule(name, enableFunc, disableFunc)
	triggerModules[name] = { enabled = false, enable = enableFunc, disable = disableFunc }
end

local function toggleModule(name, state)
	local mod = triggerModules[name]
	if not mod then return end
	if state and not mod.enabled then
		mod.enable()
		mod.enabled = true
	elseif not state and mod.enabled then
		mod.disable()
		mod.enabled = false
	end
end

local PUNCH_ANIM_IDS = {
	["108911997126897"] = true,
	["82137285150006"] = true,
	["129843313690921"] = true,
	["140703210927645"] = true,
	["136007065400978"] = true,
	["86096387000557"] = true,
	["87259391926321"] = true,
	["86709774283672"] = true,
	["108807732150251"] = true,
	["138040001965654"] = true
}

-- dynamic predict: slow target = small lead, fast target = bigger lead
local function getPredictLead(velocity)
	local horiz = Vector3.new(velocity.X, 0, velocity.Z)
	local speed = horiz.Magnitude
	local t = math.clamp(0.08 + speed * 0.014, 0.08, 0.42)
	return horiz * t
end

local function getClosestTarget(maxDist)
	local char = LP.Character
	local hrp = char and char:FindFirstChild("HumanoidRootPart")
	if not hrp then return nil end

	local kFolder = Workspace:FindFirstChild("Players") and Workspace.Players:FindFirstChild("Killers")
	if not kFolder then return nil end

	local closest, closestDist = nil, maxDist
	for _, model in ipairs(kFolder:GetChildren()) do
		if model:IsA("Model") and model ~= char then
			local targetHRP = model:FindFirstChild("HumanoidRootPart")
			local targetHum = model:FindFirstChildOfClass("Humanoid")
			if targetHRP and targetHum and targetHum.Health > 0 then
				local dist = (targetHRP.Position - hrp.Position).Magnitude
				if dist < closestDist then
					closestDist = dist
					closest = targetHRP
				end
			end
		end
	end
	return closest
end

local function aimAtTargetWithPrediction(targetHRP, duration)
	if AimPunch.Aiming then return end
	AimPunch.Aiming = true

	local char = LP.Character
	local hrp = char and char:FindFirstChild("HumanoidRootPart")
	local hum = char and char:FindFirstChildOfClass("Humanoid")
	if not hrp or not hum then AimPunch.Aiming = false return end

	hum.AutoRotate = false
	local cam = Workspace.CurrentCamera
	local startTime = tick()
	local connection
	connection = RunService.RenderStepped:Connect(function()
		local elapsed = tick() - startTime
		if elapsed >= duration or not targetHRP or not targetHRP.Parent or not hrp.Parent then
			if hum and hum.Parent then hum.AutoRotate = true end
			AimPunch.Aiming = false
			connection:Disconnect()
			return
		end

		local lead = getPredictLead(targetHRP.AssemblyLinearVelocity)
		local predictedPos = targetHRP.Position + lead
		-- aim toward body (slightly upper torso)
		local bodyPos = predictedPos + Vector3.new(0, 1.2, 0)
		local aimFlat = Vector3.new(bodyPos.X, hrp.Position.Y, bodyPos.Z)
		local charCF = CFrame.lookAt(hrp.Position, aimFlat)
		hrp.CFrame = hrp.CFrame:Lerp(charCF, 0.55)

		if AimPunch.Mode == "CamChar" and cam then
			local camPos = cam.CFrame.Position
			local camLook = CFrame.lookAt(camPos, bodyPos)
			cam.CFrame = cam.CFrame:Lerp(camLook, 0.4)
		end
	end)
end

local function setupAimPunchListener(character)
	if AimPunch.Connection then
		AimPunch.Connection:Disconnect()
		AimPunch.Connection = nil
	end

	local hum = character:WaitForChild("Humanoid", 5)
	local animator = hum and hum:WaitForChild("Animator", 5)
	if not animator then return end

	AimPunch.Connection = animator.AnimationPlayed:Connect(function(track)
		if not _G.AimPunchEnabled then return end
		local animId = track.Animation and track.Animation.AnimationId:match("%d+")
		if animId and PUNCH_ANIM_IDS[animId] then
			local targetHRP = getClosestTarget(AimPunch.Distance)
			if targetHRP then
				aimAtTargetWithPrediction(targetHRP, AimPunch.Duration)
			end
		end
	end)
end

registerTriggerModule("Punch",
	function()
		if LP.Character then setupAimPunchListener(LP.Character) end
	end,
	function()
		if AimPunch.Connection then
			AimPunch.Connection:Disconnect()
			AimPunch.Connection = nil
		end
		AimPunch.Aiming = false
	end
)

_G.BaseHitboxSize = Vector3.new(4.5, 6, 7.5)
_G.HitboxScale = 1.0
_G.HitboxOffset = -1.4
_G.HitboxColor = Color3.fromRGB(255, 255, 255)
_G.ShowOutline = true
_G.OutlineThickness = 0.01
_G.ShowHitboxVisual = true
_G.HitboxTransparency = 0.5
_G.HitboxDuration = 1.5
_G.BlockDelay = 0
_G.AutoBlockEnabled = false
_G.AutoBlockConnections = {}
_G.DragEnabled = false
_G.DragHitboxScale = 1.0
_G.DragHitboxOffset = -1.4
_G.DragHitboxDuration = 1.5
_G.DragHitboxColor = Color3.fromRGB(100, 255, 100)
_G.DragHitboxTransparency = 0.5
_G.BlockHitboxData = {}
_G.DragHitboxData = {}
_G.AutoPunchEnabled = false
_G.AimPunchEnabled = false

local infEnabled = false
local DEFAULT_SPRINT_SPEED = 28.6
local sprintSpeedEnabled = false
local sprintSpeedValue = 28.6
local function applySprintSpeed()
	if sprintSpeedEnabled then
		stamina.SprintSpeed = sprintSpeedValue
	else
		stamina.SprintSpeed = DEFAULT_SPRINT_SPEED
	end
end
local hidden = false
local Keys = { Hakari = Enum.KeyCode.B, Solve = Enum.KeyCode.H, Toggle = Enum.KeyCode.K }
local WaitingKey = { Hakari = false, Solve = false, Toggle = false }
local hakariActive = false

local function SaveConfig()
	pcall(function()
		local data = {
			infEnabled = infEnabled,
			sprintSpeedEnabled = sprintSpeedEnabled,
			espSettings = ESPSettings,
			sprintSpeed = sprintSpeedValue,
			hakariKey = Keys.Hakari.Name,
			solveKey = Keys.Solve.Name,
			toggleKey = Keys.Toggle.Name,
			autoBlock = _G.AutoBlockEnabled,
			dragEnabled = _G.DragEnabled,
			showHitbox = _G.ShowHitboxVisual,
			hitboxScale = _G.HitboxScale,
			hitboxOffset = _G.HitboxOffset,
			hitboxDuration = _G.HitboxDuration,
			autoPunch = _G.AutoPunchEnabled,
			aimPunch = _G.AimPunchEnabled,
			aimPunchMode = AimPunch.Mode,
			stabEnabled = stabEnabled,
			stabRange = stabRange,
			stabMode = stabMode,
			slashEnabled = Slash.Enabled,
			slashRange = Slash.Range,
			slashDelay = Slash.Delay,
			hitboxExt = HitboxExt.Enabled,
			hitboxExtSize = HitboxExt.Size,
			voidrushcontrol = voidrushcontrol,
			autoGen = autoGen,
			autoGenSpeed = autoGenSpeed,
			antiStun = antiStun,
			antiSlow = antiSlow,
			antiBlindness = antiBlindness,
			antiSubspace = antiSubspace,
			antiHiddenStats = antiHiddenStats,
		}
		writefile(CONFIG_FILE, HttpService:JSONEncode(data))
	end)
end

local function LoadConfig()
	pcall(function()
		if not isfile(CONFIG_FILE) then return end
		local data = HttpService:JSONDecode(readfile(CONFIG_FILE))
		if data.infEnabled ~= nil then infEnabled = data.infEnabled end
		if data.sprintSpeed then sprintSpeedValue = data.sprintSpeed end
		if data.sprintSpeedEnabled ~= nil then sprintSpeedEnabled = data.sprintSpeedEnabled end
		if data.espSettings then
			for k, v in pairs(data.espSettings) do ESPSettings[k] = v end
		end
		applySprintSpeed()
		if data.hakariKey then pcall(function() Keys.Hakari = Enum.KeyCode[data.hakariKey] end) end
		if data.solveKey then pcall(function() Keys.Solve = Enum.KeyCode[data.solveKey] end) end
		if data.toggleKey then pcall(function() Keys.Toggle = Enum.KeyCode[data.toggleKey] end) end
		if data.autoBlock ~= nil then _G.AutoBlockEnabled = data.autoBlock end
		if data.dragEnabled ~= nil then _G.DragEnabled = data.dragEnabled end
		if data.showHitbox ~= nil then _G.ShowHitboxVisual = data.showHitbox end
		if data.hitboxScale then _G.HitboxScale = data.hitboxScale end
		if data.hitboxOffset then _G.HitboxOffset = data.hitboxOffset end
		if data.hitboxDuration then _G.HitboxDuration = data.hitboxDuration end
		if data.autoPunch ~= nil then _G.AutoPunchEnabled = data.autoPunch end
		if data.aimPunch ~= nil then
			_G.AimPunchEnabled = data.aimPunch
			toggleModule("Punch", _G.AimPunchEnabled)
		end
		if data.aimPunchMode ~= nil then AimPunch.Mode = data.aimPunchMode end
		if data.stabEnabled ~= nil then stabEnabled = data.stabEnabled end
		if data.stabRange ~= nil then stabRange = data.stabRange end
		if data.stabMode ~= nil then stabMode = data.stabMode end
		if data.slashEnabled ~= nil then Slash.Enabled = data.slashEnabled end
		if data.slashRange ~= nil then Slash.Range = data.slashRange end
		if data.slashDelay ~= nil then Slash.Delay = data.slashDelay end
		if data.hitboxExtSize ~= nil then HitboxExt.Size = data.hitboxExtSize end
		if data.hitboxExt ~= nil then
			HitboxExt.Enabled = data.hitboxExt
		end
		if data.voidrushcontrol ~= nil then voidrushcontrol = data.voidrushcontrol end
		if data.autoGen ~= nil then autoGen = data.autoGen end
		if data.autoGenSpeed ~= nil then autoGenSpeed = data.autoGenSpeed end
		if data.antiStun ~= nil then antiStun = data.antiStun end
		if data.antiSlow ~= nil then antiSlow = data.antiSlow end
		if data.antiBlindness ~= nil then antiBlindness = data.antiBlindness end
		if data.antiSubspace ~= nil then antiSubspace = data.antiSubspace end
		if data.antiHiddenStats ~= nil then
			antiHiddenStats = data.antiHiddenStats
			toggleState = data.antiHiddenStats
		end
	end)
end

local MainUI, KillersFolder, SurvivorsFolder, Hitboxes

local function GetObjects()
	MainUI = LP.PlayerGui:FindFirstChild("MainUI")
	KillersFolder = Workspace:FindFirstChild("Players") and Workspace.Players:FindFirstChild("Killers")
	SurvivorsFolder = Workspace:FindFirstChild("Players") and Workspace.Players:FindFirstChild("Survivors")
	Hitboxes = Workspace:FindFirstChild("Hitboxes") or Instance.new("Folder", Workspace)
	Hitboxes.Name = "Hitboxes"
end
GetObjects()

local function getActionButton(name)
	if not MainUI then return nil end
	local container = MainUI:FindFirstChild("AbilityContainer")
	if not container then return nil end
	return container:FindFirstChild(name)
end

local function getBlockButton() return getActionButton("Block") or getActionButton("Guard") end
local function getPunchButton() return getActionButton("Punch") or getActionButton("Attack") or getActionButton("Slap") end

local function pressButton(btn)
	if not btn then return false end
	local cd = btn:FindFirstChild("CooldownTime")
	if cd and cd.Text ~= "" then return false end
	pcall(function() btn:Activate() end)
	for _, conn in ipairs(getconnections(btn.MouseButton1Click) or {}) do
		pcall(function() conn:Fire() end)
	end
	return true
end

local function pressBlock() return pressButton(getBlockButton()) end
local function pressPunch() return pressButton(getPunchButton()) end

local function CreateBlockHitboxVisual()
	local part = Instance.new("Part")
	part.Name = "AttackHitboxVisual"
	part.Size = _G.BaseHitboxSize * _G.HitboxScale
	part.Color = _G.HitboxColor
	part.Material = Enum.Material.ForceField
	part.Transparency = 1 - _G.HitboxTransparency
	part.Anchored = true
	part.CanCollide = false
	part.CastShadow = false
	part.Parent = Hitboxes
	if _G.ShowOutline then
		local outline = Instance.new("SelectionBox")
		outline.Adornee = part
		outline.Color3 = _G.HitboxColor
		outline.LineThickness = _G.OutlineThickness
		outline.Transparency = 0.1
		outline.Visible = _G.ShowHitboxVisual
		outline.Parent = part
	end
	return part
end

local function UpdateBlockHitboxPosition(part, queryHitbox)
	if part and queryHitbox then
		part.CFrame = queryHitbox.CFrame * CFrame.new(0, 0, _G.HitboxOffset)
		part.Size = _G.BaseHitboxSize * _G.HitboxScale
		part.Color = _G.HitboxColor
		part.Transparency = 1 - _G.HitboxTransparency
		local outline = part:FindFirstChildOfClass("SelectionBox")
		if outline then
			outline.Color3 = _G.HitboxColor
			outline.LineThickness = _G.OutlineThickness
			outline.Visible = _G.ShowOutline and _G.ShowHitboxVisual
		end
	end
end

local function ClearKillerBlockHitbox(Killer)
	local data = _G.BlockHitboxData[Killer]
	if data then
		if data.visualPart then data.visualPart:Destroy() end
		if data.visualConnection then data.visualConnection:Disconnect() end
		if data.heartbeatConnection then data.heartbeatConnection:Disconnect() end
		_G.BlockHitboxData[Killer] = nil
	end
end

local OverlapParamsObj = OverlapParams.new()
OverlapParamsObj.MaxParts = 1
OverlapParamsObj.FilterType = Enum.RaycastFilterType.Include

local function StartBlockDetection(Killer, queryHitbox)
	ClearKillerBlockHitbox(Killer)
	local visualPart = CreateBlockHitboxVisual()
	UpdateBlockHitboxPosition(visualPart, queryHitbox)

	local visualConnection = RunService.Heartbeat:Connect(function()
		if visualPart and visualPart.Parent and queryHitbox and queryHitbox.Parent then
			UpdateBlockHitboxPosition(visualPart, queryHitbox)
		else
			visualConnection:Disconnect()
		end
	end)

	local data = {
		visualPart = visualPart,
		visualConnection = visualConnection,
		heartbeatConnection = nil
	}
	_G.BlockHitboxData[Killer] = data

	local localChar = LP.Character
	if not localChar or not SurvivorsFolder or localChar.Parent ~= SurvivorsFolder then
		task.delay(_G.HitboxDuration, function() ClearKillerBlockHitbox(Killer) end)
		return
	end
	local localQuery = localChar:FindFirstChild("QueryHitbox")
	if not localQuery then
		task.delay(_G.HitboxDuration, function() ClearKillerBlockHitbox(Killer) end)
		return
	end

	local hitboxSize = _G.BaseHitboxSize * _G.HitboxScale
	local startTime = tick()

	local heartbeatPart = Instance.new("Part")
	heartbeatPart.Size = hitboxSize
	heartbeatPart.CanCollide = false
	heartbeatPart.Anchored = true
	heartbeatPart.Transparency = 1
	heartbeatPart.Parent = Hitboxes

	local heartbeatConn
	heartbeatConn = RunService.Heartbeat:Connect(function()
		if not _G.AutoBlockEnabled then
			heartbeatConn:Disconnect()
			heartbeatPart:Destroy()
			ClearKillerBlockHitbox(Killer)
			return
		end
		if tick() - startTime > _G.HitboxDuration then
			heartbeatConn:Disconnect()
			heartbeatPart:Destroy()
			ClearKillerBlockHitbox(Killer)
			return
		end
		if not queryHitbox or not queryHitbox.Parent then
			heartbeatConn:Disconnect()
			heartbeatPart:Destroy()
			ClearKillerBlockHitbox(Killer)
			return
		end

		heartbeatPart.CFrame = queryHitbox.CFrame * CFrame.new(0, 0, _G.HitboxOffset)
		OverlapParamsObj.FilterDescendantsInstances = { localQuery }
		local hits = Workspace:GetPartsInPart(heartbeatPart, OverlapParamsObj)
		if #hits > 0 then
			pressBlock()
			heartbeatConn:Disconnect()
			heartbeatPart:Destroy()
			ClearKillerBlockHitbox(Killer)
		end
	end)
	data.heartbeatConnection = heartbeatConn
end

local function HandleKiller_Attribute(Killer)
	if not Players:GetPlayerFromCharacter(Killer) then return end
	local queryHitbox = Killer:FindFirstChild("QueryHitbox")
	if not queryHitbox then return end
	local blockLocked = false
	local abilityConn, abilitiessConn

	local function doBlock()
		if _G.AutoBlockEnabled then
			StartBlockDetection(Killer, queryHitbox)
		end
	end

	abilityConn = Killer:GetAttributeChangedSignal("AbilityLastUsed"):Connect(function()
		if not blockLocked then doBlock() else blockLocked = false end
	end)
	abilitiessConn = Killer:GetAttributeChangedSignal("AbilitiesUsed"):Connect(function()
		blockLocked = true
	end)

	table.insert(_G.AutoBlockConnections, {
		Disconnect = function()
			abilityConn:Disconnect()
			abilitiessConn:Disconnect()
		end
	})
end

local function EnableAutoBlock()
	if _G.AutoBlockEnabled then
		for _, con in ipairs(_G.AutoBlockConnections) do pcall(con.Disconnect, con) end
		_G.AutoBlockConnections = {}
		GetObjects()
		if KillersFolder then
			for _, killer in ipairs(KillersFolder:GetChildren()) do HandleKiller_Attribute(killer) end
			local childCon = KillersFolder.ChildAdded:Connect(HandleKiller_Attribute)
			table.insert(_G.AutoBlockConnections, { Disconnect = function() childCon:Disconnect() end })
		end
	end
end

local function DisableAutoBlock()
	for _, con in ipairs(_G.AutoBlockConnections) do pcall(con.Disconnect, con) end
	_G.AutoBlockConnections = {}
	for killer, data in pairs(_G.BlockHitboxData) do
		if data.visualPart then data.visualPart:Destroy() end
		if data.visualConnection then data.visualConnection:Disconnect() end
		if data.heartbeatConnection then data.heartbeatConnection:Disconnect() end
	end
	_G.BlockHitboxData = {}
end

local function IsRagdoll(model)
	local ragdolls = Workspace:FindFirstChild("Ragdolls")
	return ragdolls and (model:IsDescendantOf(ragdolls) or model.Parent == ragdolls)
end

local function IsSpectating(player)
	if not player then return false end
	local pf = Workspace:FindFirstChild("Players")
	if not pf then return false end
	local sp = pf:FindFirstChild("Spectating")
	return sp and sp:FindFirstChild(player.Name) ~= nil
end

local function createPlayerESP(model, color, isKiller)
	if model:FindFirstChild("c00lSaken_Highlight") then return end
	if IsRagdoll(model) then return end
	local hrp = model:FindFirstChild("HumanoidRootPart")
	if not hrp then return end
	local highlight = Instance.new("Highlight")
	highlight.Name = "c00lSaken_Highlight"
	highlight.Adornee = model
	highlight.FillColor = color
	highlight.OutlineColor = color
	highlight.FillTransparency = isKiller and ESPSettings.killerFillTransparency or ESPSettings.survivorFillTransparency
	highlight.OutlineTransparency = isKiller and ESPSettings.killerOutlineTransparency or ESPSettings.survivorOutlineTransparency
	highlight.DepthMode = Enum.HighlightDepthMode.AlwaysOnTop
	highlight.Parent = model
end

local function removePlayerESP(model)
	pcall(function()
		local h = model:FindFirstChild("c00lSaken_Highlight")
		if h then h:Destroy() end
	end)
end

local function CreateObjectESP(model, color)
	if not model then return end
	if model:FindFirstChild("TAOWARE_Highlight") then return end
	if IsRagdoll(model) then return end
	local highlight = Instance.new("Highlight")
	highlight.Name = "TAOWARE_Highlight"
	highlight.Adornee = model
	highlight.FillColor = color
	highlight.OutlineColor = color
	highlight.FillTransparency = 0.7
	highlight.OutlineTransparency = 0.3
	highlight.DepthMode = Enum.HighlightDepthMode.AlwaysOnTop
	highlight.Parent = model
end

local function RemoveObjectESP(model)
	if not model then return end
	pcall(function()
		if model:FindFirstChild("TAOWARE_Highlight") then
			model.TAOWARE_Highlight:Destroy()
		end
	end)
end

local function UpdateESP()
	local pf = Workspace:FindFirstChild("Players")
	if pf then
		local killersFolder = pf:FindFirstChild("Killers")
		local survivorsFolder = pf:FindFirstChild("Survivors")
		if killersFolder then
			for _, killer in ipairs(killersFolder:GetChildren()) do
				if killer == LP.Character then continue end
				if IsRagdoll(killer) then removePlayerESP(killer) continue end
				local player = Players:GetPlayerFromCharacter(killer)
				if not player or IsSpectating(player) then removePlayerESP(killer) continue end
				if ESPSettings.killerESP then
					if not killer:FindFirstChild("c00lSaken_Highlight") then
						createPlayerESP(killer, ESPSettings.killerColor, true)
					end
				else
					removePlayerESP(killer)
				end
			end
		end
		if survivorsFolder then
			for _, survivor in ipairs(survivorsFolder:GetChildren()) do
				if survivor == LP.Character then continue end
				if IsRagdoll(survivor) then removePlayerESP(survivor) continue end
				local player = Players:GetPlayerFromCharacter(survivor)
				if not player or IsSpectating(player) then removePlayerESP(survivor) continue end
				if ESPSettings.playerESP then
					if not survivor:FindFirstChild("c00lSaken_Highlight") then
						createPlayerESP(survivor, ESPSettings.survivorColor, false)
					end
				else
					removePlayerESP(survivor)
				end
			end
		end
	end

	mapObj = Workspace:FindFirstChild("Map")
	if mapObj then MapFolder = mapObj:FindFirstChild("Ingame") end
	if not MapFolder then return end
	local ingame = MapFolder

	if ingame:FindFirstChild("Map") then
		for _, gen in ipairs(ingame.Map:GetChildren()) do
			if gen:IsA("Model") and gen.Name:lower():find("generator") and gen.Name ~= "FakeGenerator" then
				if IsRagdoll(gen) then RemoveObjectESP(gen) continue end
				local progress = gen:FindFirstChild("Progress")
				if ESPSettings.generatorESP and progress and progress.Value < 100 and not gen:FindFirstChild("TAOWARE_Highlight") then
					CreateObjectESP(gen, ESPSettings.generatorColor)
				elseif not ESPSettings.generatorESP or (progress and progress.Value >= 100) then
					RemoveObjectESP(gen)
				end
			end
		end
		for _, item in ipairs(ingame.Map:GetDescendants()) do
			if item.Name == "ItemRoot" and item.Parent and item.Parent:IsA("Model") then
				local itemModel = item.Parent
				if ESPSettings.itemESP and not itemModel:FindFirstChild("TAOWARE_Highlight") then
					CreateObjectESP(itemModel, ESPSettings.itemColor)
				elseif not ESPSettings.itemESP then
					RemoveObjectESP(itemModel)
				end
			end
		end
	end

	for _, pizza in ipairs(ingame:GetChildren()) do
		if pizza.Name == "Pizza" and pizza:IsA("BasePart") then
			if ESPSettings.pizzaEsp and not pizza:FindFirstChild("TAOWARE_Highlight") then
				CreateObjectESP(pizza, ESPSettings.pizzaColor)
			elseif not ESPSettings.pizzaEsp then
				RemoveObjectESP(pizza)
			end
		end
	end

	for _, delivery in ipairs(ingame:GetChildren()) do
		if delivery:IsA("Model") and table.find(DummyNames, delivery.Name) then
			if ESPSettings.pizzaDeliveryEsp and not delivery:FindFirstChild("TAOWARE_Highlight") then
				if delivery:FindFirstChild("HumanoidRootPart") then
					CreateObjectESP(delivery, ESPSettings.pizzaDeliveryColor)
				end
			elseif not ESPSettings.pizzaDeliveryEsp then
				RemoveObjectESP(delivery)
			end
		end
	end

	for _, zombie in ipairs(ingame:GetChildren()) do
		if zombie.Name == "1x1x1x1Zombie" and zombie:IsA("Model") then
			if ESPSettings.zombieEsp and not zombie:FindFirstChild("TAOWARE_Highlight") then
				if zombie:FindFirstChild("HumanoidRootPart") then
					CreateObjectESP(zombie, ESPSettings.zombieColor)
				end
			elseif not ESPSettings.zombieEsp then
				RemoveObjectESP(zombie)
			end
		end
	end

	for _, obj in ipairs(ingame:GetChildren()) do
		if obj.Name:match("TaphTripwire$") and obj:IsA("Model") then
			if ESPSettings.taphTripwireEsp and not obj:FindFirstChild("TAOWARE_Highlight") then
				CreateObjectESP(obj, ESPSettings.taphTripwireColor)
			elseif not ESPSettings.taphTripwireEsp then
				RemoveObjectESP(obj)
			end
		end
		if obj.Name == "SubspaceTripmine" and obj:IsA("Model") then
			if ESPSettings.tripMineEsp and not obj:FindFirstChild("TAOWARE_Highlight") then
				CreateObjectESP(obj, ESPSettings.tripMineColor)
			elseif not ESPSettings.tripMineEsp then
				RemoveObjectESP(obj)
			end
		end
		if obj.Name == "GraffitiCL" and obj:IsA("BasePart") then
			if ESPSettings.graffitiEsp and not obj:FindFirstChild("TAOWARE_Highlight") then
				CreateObjectESP(obj, ESPSettings.graffitiColor)
			elseif not ESPSettings.graffitiEsp then
				RemoveObjectESP(obj)
			end
		end
	end

	for _, obj in ipairs(ingame:GetDescendants()) do
		if obj and obj.Name and tostring(obj.Name):lower():find("respawnlocation") then
			local target = obj:IsA("Model") and obj or obj:IsA("BasePart") and obj or (obj:FindFirstAncestorOfClass("Model") or obj)
			if not target or IsRagdoll(target) then continue end
			if ESPSettings.twoTimeRespawnEsp and not target:FindFirstChild("TAOWARE_Highlight") then
				CreateObjectESP(target, ESPSettings.twoTimeRespawnColor)
			elseif not ESPSettings.twoTimeRespawnEsp then
				RemoveObjectESP(target)
			end
		end
	end

	for _, obj in ipairs(ingame:GetDescendants()) do
		if obj:IsA("Model") then
			local nameLower = obj.Name:lower()
			if nameLower == "groundbulbmodel" then
				if ESPSettings.azureGroundBulbESP then
					if not obj:FindFirstChild("TAOWARE_Highlight") then
						CreateObjectESP(obj, ESPSettings.azureGroundBulbColor)
					end
				else
					RemoveObjectESP(obj)
				end
			elseif nameLower == "vinemodel" then
				if ESPSettings.azureVineESP then
					if not obj:FindFirstChild("TAOWARE_Highlight") then
						CreateObjectESP(obj, ESPSettings.azureVineColor)
					end
				else
					RemoveObjectESP(obj)
				end
			end
		end
	end
end

local espLoopRunning = true
task.spawn(function()
	while espLoopRunning do
		task.wait(0.5)
		pcall(UpdateESP)
	end
end)


local paths = {"HideKillerWins", "HidePlaytime", "HideSurvivorWins"}
local function saveOriginals(p)
	originalValues[p.UserId] = originalValues[p.UserId] or {}
	pcall(function()
		for _, k in ipairs(paths) do
			local v = p.PlayerData.Settings.Privacy:FindFirstChild(k)
			if v then originalValues[p.UserId][k] = v.Value end
		end
	end)
end
local function reveal(p)
	pcall(function()
		for _, k in ipairs(paths) do
			local v = p.PlayerData.Settings.Privacy:FindFirstChild(k)
			if v then v.Value = false end
		end
	end)
end
local function restore(p)
	if not originalValues[p.UserId] then return end
	pcall(function()
		for k, val in pairs(originalValues[p.UserId]) do
			local v = p.PlayerData.Settings.Privacy:FindFirstChild(k)
			if v then v.Value = val end
		end
	end)
end
local function hiddenStatsFunc(on)
	for _, p in ipairs(Players:GetPlayers()) do
		if on then
			saveOriginals(p)
			reveal(p)
		else
			restore(p)
		end
	end
end
Players.PlayerAdded:Connect(function(p)
	if toggleState then
		saveOriginals(p)
		reveal(p)
	end
end)

task.spawn(function()
	while true do
		task.wait()
		if antiStun then
			pcall(function()
				local char = LP.Character
				if char and char:FindFirstChild("SpeedMultipliers") then
					local s = char.SpeedMultipliers:FindFirstChild("Stunned")
					if s then s.Value = 1.2 end
				end
			end)
		end
		if antiSlow then
			pcall(function()
				local char = LP.Character
				if char and char:FindFirstChild("SpeedMultipliers") then
					for _, m in ipairs(char.SpeedMultipliers:GetChildren()) do
						if m.Value < 1 then m.Value = 1.2 end
					end
				end
			end)
		end
		if antiBlindness then
			pcall(function()
				local b = Lighting:FindFirstChild("BlindnessBlur")
				if b then b:Destroy() end
			end)
		end
		if antiSubspace then
			pcall(function()
				local a = Lighting:FindFirstChild("SubspaceVFXBlur")
				local b = Lighting:FindFirstChild("SubspaceVFXColorCorrection")
				if a then a:Destroy() end
				if b then b:Destroy() end
			end)
		end
	end
end)

local function clickDaggerButton()
	local gui = LP.PlayerGui:FindFirstChild("MainUI")
	local btn = gui and gui:FindFirstChild("AbilityContainer") and gui.AbilityContainer:FindFirstChild("Dagger")
	if not btn or btn.BackgroundTransparency == 0 then return end
	for _, c in ipairs(getconnections(btn.MouseButton1Click) or {}) do pcall(function() c:Fire() end) end
	pcall(function() btn:Activate() end)
end

local function clickSlashButton()
	local gui = LP.PlayerGui:FindFirstChild("MainUI")
	local btn = gui and gui:FindFirstChild("AbilityContainer") and gui.AbilityContainer:FindFirstChild("Slash")
	if not btn or btn.BackgroundTransparency == 0 then return end
	for _, c in ipairs(getconnections(btn.MouseButton1Click) or {}) do pcall(function() c:Fire() end) end
	pcall(function() btn:Activate() end)
end

local function isBehind(hrp, targetHRP)
	if (hrp.Position - targetHRP.Position).Magnitude > stabRange then return false end
	if stabMode == "Around" then return true end
	local dir = -targetHRP.CFrame.LookVector
	return (hrp.Position - targetHRP.Position):Dot(dir) > 0.5
end

local function isSlashInRange(hrp, targetHRP)
	return (hrp.Position - targetHRP.Position).Magnitude <= Slash.Range
end

local slashRayParams = RaycastParams.new()
slashRayParams.FilterType = Enum.RaycastFilterType.Exclude
slashRayParams.IgnoreWater = true

local function hasWallBetween(fromPos, toPos, ignoreModels)
	if not fromPos or not toPos then return true end
	local ignore = { LP.Character }
	if ignoreModels then
		for _, v in ipairs(ignoreModels) do table.insert(ignore, v) end
	end
	local pf = Workspace:FindFirstChild("Players")
	if pf then table.insert(ignore, pf) end
	slashRayParams.FilterDescendantsInstances = ignore
	local delta = toPos - fromPos
	if delta.Magnitude < 0.1 then return false end
	local result = Workspace:Raycast(fromPos, delta, slashRayParams)
	if not result then return false end
	return (result.Position - fromPos).Magnitude < delta.Magnitude - 1.5
end

local function isLocalKiller()
	local char = LP.Character
	if not char then return false end
	local pf = Workspace:FindFirstChild("Players")
	local kf = pf and pf:FindFirstChild("Killers")
	if kf and char.Parent == kf then return true end
	return false
end

local autoPunchRunning = true
task.spawn(function()
	while autoPunchRunning do
		task.wait(0.1)
		if _G.AutoPunchEnabled then
			pcall(function()
				local char = LP.Character
				local hrp = char and char:FindFirstChild("HumanoidRootPart")
				local kFolder = Workspace:FindFirstChild("Players") and Workspace.Players:FindFirstChild("Killers")
				if hrp and kFolder then
					local killerInRange = false
					for _, killer in ipairs(kFolder:GetChildren()) do
						if killer:IsA("Model") and killer:FindFirstChild("HumanoidRootPart") then
							local kHRP = killer.HumanoidRootPart
							local dist = (kHRP.Position - hrp.Position).Magnitude
							if dist <= 10 then
								killerInRange = true
								break
							end
						end
					end
					if killerInRange then
						local tool = char:FindFirstChildOfClass("Tool")
						if tool then
							tool:Activate()
						else
							pressPunch()
						end
					end
				end
			end)
		end
	end
end)

local stabConnection
stabConnection = RunService.Heartbeat:Connect(function()
	if not stabEnabled then return end
	if tick() - lastBackstabTime < BACKSTAB_COOLDOWN then return end
	local char = LP.Character
	local hrp = char and char:FindFirstChild("HumanoidRootPart")
	if not hrp then return end
	local kFolder = Workspace:FindFirstChild("Players") and Workspace.Players:FindFirstChild("Killers")
	if not kFolder then return end

	for _, name in ipairs(killerNames) do
		local killer = kFolder:FindFirstChild(name)
		local kHRP = killer and killer:FindFirstChild("HumanoidRootPart")
		if kHRP and isBehind(hrp, kHRP) then
			lastBackstabTime = tick()
			local start = tick()
			local offset = kHRP.CFrame.LookVector * BACKSTAB_DISTANCE
			clickDaggerButton()
			local conn
			conn = RunService.Heartbeat:Connect(function()
				if tick() - start > BACKSTAB_HOLD_TIME or not killer.Parent then
					conn:Disconnect()
					return
				end
				hrp.CFrame = CFrame.new(kHRP.Position - offset, kHRP.Position)
			end)
			break
		end
	end
end)

Slash.Connection = RunService.Heartbeat:Connect(function()
	if not Slash.Enabled or Slash.Busy then return end
	if isLocalKiller() then return end
	if tick() - Slash.LastTime < Slash.Cooldown then return end
	local char = LP.Character
	local hrp = char and char:FindFirstChild("HumanoidRootPart")
	if not hrp then return end
	local kFolder = Workspace:FindFirstChild("Players") and Workspace.Players:FindFirstChild("Killers")
	if not kFolder then return end

	for _, name in ipairs(killerNames) do
		local killer = kFolder:FindFirstChild(name)
		local kHRP = killer and killer:FindFirstChild("HumanoidRootPart")
		if kHRP and isSlashInRange(hrp, kHRP) and not hasWallBetween(hrp.Position, kHRP.Position, { killer }) then
			Slash.LastTime = tick()
			Slash.Busy = true
			task.spawn(function()
				local hum = char:FindFirstChildOfClass("Humanoid")
				if hum then hum.AutoRotate = false end

				local start = tick()
				local holdConn
				local function stopAim()
					if holdConn then
						holdConn:Disconnect()
						holdConn = nil
					end
					local h = (LP.Character and LP.Character:FindFirstChildOfClass("Humanoid")) or hum
					if h then h.AutoRotate = true end
					Slash.Busy = false
				end

				holdConn = RunService.Heartbeat:Connect(function()
					if not killer.Parent or not kHRP.Parent or not hrp.Parent then
						stopAim()
						return
					end
					if tick() - start > Slash.HoldTime then
						stopAim()
						return
					end
					if hasWallBetween(hrp.Position, kHRP.Position, { killer }) then return end
					local predicted = kHRP.Position + getPredictLead(kHRP.AssemblyLinearVelocity)
					local toPred = hrp.Position - predicted
					local dir
					if toPred.Magnitude < 0.1 then
						dir = -kHRP.CFrame.LookVector
					else
						dir = Vector3.new(toPred.X, 0, toPred.Z)
						if dir.Magnitude < 0.1 then
							dir = -kHRP.CFrame.LookVector
						else
							dir = dir.Unit
						end
					end
					local nearPos = predicted + dir * Slash.Distance
					nearPos = Vector3.new(nearPos.X, kHRP.Position.Y, nearPos.Z)
					local aimPos = Vector3.new(predicted.X, hrp.Position.Y, predicted.Z)
					if hasWallBetween(hrp.Position, nearPos, { killer }) then
						hrp.CFrame = CFrame.lookAt(hrp.Position, aimPos)
						return
					end
					hrp.CFrame = CFrame.lookAt(nearPos, aimPos)
				end)

				task.wait(Slash.Delay)
				if killer.Parent and kHRP.Parent and hrp.Parent and not hasWallBetween(hrp.Position, kHRP.Position, { killer }) then
					clickSlashButton()
				end
				local remain = Slash.HoldTime - (tick() - start)
				if remain > 0 then task.wait(remain) end
				stopAim()
			end)
			break
		end
	end
end)

local autoBlockMonitor = true
task.spawn(function()
	while autoBlockMonitor do
		task.wait(1)
		if not _G.AutoBlockEnabled then continue end
		local pf = Workspace:FindFirstChild("Players")
		local kf = pf and pf:FindFirstChild("Killers")
		if kf and kf ~= KillersFolder then
			KillersFolder = kf
			DisableAutoBlock()
			EnableAutoBlock()
		end
	end
end)

LoadConfig()
if toggleState then
	task.defer(function()
		task.wait(1)
		hiddenStatsFunc(true)
	end)
end

-- ============================================================
-- UI แนวนอน + แท็บหมวดหมู่ (ไม่ต้องกดลูกศร) + ภาษาไทย
-- ============================================================

local gui = Instance.new("ScreenGui")
gui.Name = "c00lSaken"
gui.ResetOnSpawn = false
gui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
gui.Parent = LP.PlayerGui

local RED = Color3.fromRGB(255, 0, 0)
local BG = Color3.fromRGB(15, 15, 15)
local BG2 = Color3.fromRGB(25, 25, 25)
local TXT = Color3.fromRGB(255, 255, 255)
local ACCENT = Color3.fromRGB(255, 50, 50)

-- ขนาดหลัก (แนวนอน)
local MAIN_W = 520
local MAIN_H = 340

local main = Instance.new("Frame")
main.Name = "Main"
main.Size = UDim2.new(0, MAIN_W, 0, MAIN_H)
main.Position = UDim2.new(0.5, -MAIN_W/2, 0.5, -MAIN_H/2)
main.BackgroundColor3 = BG
main.BorderColor3 = RED
main.BorderSizePixel = 2
main.BackgroundTransparency = 1
main.Active = true
main.Parent = gui

-- หัวเรื่อง
local titleBar = Instance.new("Frame")
titleBar.Size = UDim2.new(1, 0, 0, 32)
titleBar.BackgroundColor3 = BG
titleBar.BorderColor3 = RED
titleBar.BorderSizePixel = 2
titleBar.BackgroundTransparency = 1
titleBar.Parent = main

local title = Instance.new("TextLabel")
title.Size = UDim2.new(1, -80, 1, 0)
title.Position = UDim2.new(0, 8, 0, 0)
title.BackgroundTransparency = 1
title.Text = "c00lSaken  |  ภาษาไทย"
title.TextColor3 = TXT
title.Font = Enum.Font.SourceSansBold
title.TextSize = 18
title.TextXAlignment = Enum.TextXAlignment.Left
title.TextTransparency = 1
title.Parent = titleBar

local closeBtn = Instance.new("TextButton")
closeBtn.Size = UDim2.new(0, 60, 0, 24)
closeBtn.Position = UDim2.new(1, -68, 0.5, -12)
closeBtn.BackgroundColor3 = BG
closeBtn.BorderColor3 = RED
closeBtn.BorderSizePixel = 2
closeBtn.Text = "ปิด"
closeBtn.TextColor3 = TXT
closeBtn.Font = Enum.Font.SourceSansBold
closeBtn.TextSize = 14
closeBtn.BackgroundTransparency = 1
closeBtn.TextTransparency = 1
closeBtn.Parent = titleBar

-- แถบแท็บหมวดหมู่ (แนวนอน)
local tabBar = Instance.new("Frame")
tabBar.Size = UDim2.new(1, 0, 0, 36)
tabBar.Position = UDim2.new(0, 0, 0, 32)
tabBar.BackgroundColor3 = BG2
tabBar.BorderColor3 = RED
tabBar.BorderSizePixel = 1
tabBar.BackgroundTransparency = 1
tabBar.Parent = main

local tabNames = {
	"สตามินา",
	"ESP",
	"เบ็ดเตล็ด",
	"บล็อก/ต่อย",
	"แทง",
	"บทบาท",
	"ป้องกัน",
	"อันตราย"
}

local tabButtons = {}
local pages = {}
local currentTab = 1

local content = Instance.new("Frame")
content.Size = UDim2.new(1, 0, 1, -100)
content.Position = UDim2.new(0, 0, 0, 68)
content.BackgroundColor3 = BG
content.BorderColor3 = RED
content.BorderSizePixel = 2
content.BackgroundTransparency = 1
content.ClipsDescendants = true
content.Parent = main

-- ฟังก์ชันสร้างปุ่ม/ป้าย/กล่อง
local function Btn(parent, text, y, h)
	h = h or 26
	local b = Instance.new("TextButton")
	b.Size = UDim2.new(1, -16, 0, h)
	b.Position = UDim2.new(0, 8, 0, y)
	b.BackgroundColor3 = BG2
	b.BorderColor3 = RED
	b.BorderSizePixel = 1
	b.Text = text
	b.TextColor3 = TXT
	b.Font = Enum.Font.SourceSansBold
	b.TextSize = 13
	b.BackgroundTransparency = 1
	b.TextTransparency = 1
	b.Parent = parent
	return b
end

local function Lbl(parent, text, y)
	local l = Instance.new("TextLabel")
	l.Size = UDim2.new(1, -16, 0, 18)
	l.Position = UDim2.new(0, 8, 0, y)
	l.BackgroundTransparency = 1
	l.Text = text
	l.TextColor3 = Color3.fromRGB(200, 200, 200)
	l.Font = Enum.Font.SourceSans
	l.TextSize = 13
	l.TextXAlignment = Enum.TextXAlignment.Left
	l.TextTransparency = 1
	l.Parent = parent
	return l
end

local function Box(parent, default, y)
	local t = Instance.new("TextBox")
	t.Size = UDim2.new(1, -16, 0, 26)
	t.Position = UDim2.new(0, 8, 0, y)
	t.BackgroundColor3 = BG2
	t.BorderColor3 = RED
	t.BorderSizePixel = 1
	t.Text = tostring(default)
	t.TextColor3 = TXT
	t.Font = Enum.Font.SourceSans
	t.TextSize = 14
	t.ClearTextOnFocus = false
	t.BackgroundTransparency = 1
	t.TextTransparency = 1
	t.Parent = parent
	return t
end

local function createPage()
	local p = Instance.new("ScrollingFrame")
	p.Size = UDim2.new(1, 0, 1, 0)
	p.CanvasSize = UDim2.new(0, 0, 0, 0)
	p.ScrollBarThickness = 5
	p.AutomaticCanvasSize = Enum.AutomaticSize.Y
	p.BackgroundTransparency = 1
	p.BorderSizePixel = 0
	p.Visible = false
	p.Parent = content
	return p
end

-- สร้างแท็บปุ่ม
local tabW = MAIN_W / #tabNames
for i, name in ipairs(tabNames) do
	local tb = Instance.new("TextButton")
	tb.Size = UDim2.new(0, tabW - 2, 1, -4)
	tb.Position = UDim2.new(0, (i-1)*tabW + 1, 0, 2)
	tb.BackgroundColor3 = (i == 1) and ACCENT or BG2
	tb.BorderColor3 = RED
	tb.BorderSizePixel = 1
	tb.Text = name
	tb.TextColor3 = TXT
	tb.Font = Enum.Font.SourceSansBold
	tb.TextSize = 12
	tb.BackgroundTransparency = 1
	tb.TextTransparency = 1
	tb.Parent = tabBar
	tabButtons[i] = tb

	local page = createPage()
	pages[i] = page

	tb.MouseButton1Click:Connect(function()
		currentTab = i
		for j, p in ipairs(pages) do
			p.Visible = (j == i)
		end
		for j, b in ipairs(tabButtons) do
			b.BackgroundColor3 = (j == i) and ACCENT or BG2
		end
	end)
end
pages[1].Visible = true

local UI = {}

-- ========== หน้า 1: สตามินา ==========
local page1 = pages[1]
Lbl(page1, "ความเร็ววิ่ง / สตามินา", 6)
UI.speedBox = Box(page1, sprintSpeedValue, 26)
UI.sprintSpeedBtn = Btn(page1, "ความเร็ววิ่ง " .. (sprintSpeedEnabled and "เปิด" or "ปิด"), 58)
UI.infButton = Btn(page1, "สตามินาไม่จำกัด " .. (infEnabled and "เปิด" or "ปิด"), 90)

-- ========== หน้า 2: ESP ==========
local page2 = pages[2]
Lbl(page2, "ESP (มองทะลุ)", 4)
UI.killerESPBtn = Btn(page2, "ESP ฆาตกร " .. (ESPSettings.killerESP and "เปิด" or "ปิด"), 24)
UI.survivorESPBtn = Btn(page2, "ESP ผู้รอด " .. (ESPSettings.playerESP and "เปิด" or "ปิด"), 54)
UI.genESPBtn = Btn(page2, "ESP เครื่องปั่น " .. (ESPSettings.generatorESP and "เปิด" or "ปิด"), 84)
UI.itemESPBtn = Btn(page2, "ESP ไอเทม " .. (ESPSettings.itemESP and "เปิด" or "ปิด"), 114)
UI.pizzaESPBtn = Btn(page2, "ESP พิซซ่า " .. (ESPSettings.pizzaEsp and "เปิด" or "ปิด"), 144)
UI.deliveryESPBtn = Btn(page2, "ESP ส่งพิซซ่า " .. (ESPSettings.pizzaDeliveryEsp and "เปิด" or "ปิด"), 174)
UI.zombieESPBtn = Btn(page2, "ESP ซอมบี้ " .. (ESPSettings.zombieEsp and "เปิด" or "ปิด"), 204)
UI.tripwireESPBtn = Btn(page2, "ESP สายดัก " .. (ESPSettings.taphTripwireEsp and "เปิด" or "ปิด"), 234)
UI.mineESPBtn = Btn(page2, "ESP ระเบิด " .. (ESPSettings.tripMineEsp and "เปิด" or "ปิด"), 264)
UI.respawnESPBtn = Btn(page2, "ESP จุดเกิด " .. (ESPSettings.twoTimeRespawnEsp and "เปิด" or "ปิด"), 294)
UI.graffitiESPBtn = Btn(page2, "ESP กราฟฟิตี้ " .. (ESPSettings.graffitiEsp and "เปิด" or "ปิด"), 324)
UI.groundBulbESPBtn = Btn(page2, "ESP หัวหลอด " .. (ESPSettings.azureGroundBulbESP and "เปิด" or "ปิด"), 354)
UI.vineESPBtn = Btn(page2, "ESP เถาวัลย์ " .. (ESPSettings.azureVineESP and "เปิด" or "ปิด"), 384)

-- ========== หน้า 3: เบ็ดเตล็ด ==========
local page3 = pages[3]
Lbl(page3, "เบ็ดเตล็ด", 4)
UI.keyButton = Btn(page3, "ปุ่มฮาคาริ: " .. Keys.Hakari.Name, 24)
UI.solveButton = Btn(page3, "แก้ปริศนาเดี๋ยวนี้", 54)
UI.solveKeyBtn = Btn(page3, "ปุ่มแก้ปริศนา: " .. Keys.Solve.Name, 84)
UI.autoGenBtn = Btn(page3, "ออโต้เครื่องปั่น " .. (autoGen and "เปิด" or "ปิด"), 114)
Lbl(page3, "ความเร็วออโต้ (0.01-1)", 144)
UI.genSpeedBox = Box(page3, autoGenSpeed, 164)

-- ========== หน้า 4: บล็อก & ต่อย ==========
local page4 = pages[4]
Lbl(page4, "บล็อกอัตโนมัติ & ต่อย", 4)
UI.autoBlockBtn = Btn(page4, "บล็อกอัตโนมัติ " .. (_G.AutoBlockEnabled and "เปิด" or "ปิด"), 24)
UI.showHitboxBtn = Btn(page4, "แสดงฮิตบ็อกซ์ " .. (_G.ShowHitboxVisual and "เปิด" or "ปิด"), 54)
Lbl(page4, "ขนาด | ออฟเซ็ต Z | ระยะเวลา", 84)
UI.scaleBox = Box(page4, _G.HitboxScale, 104)
UI.offsetBox = Box(page4, _G.HitboxOffset, 134)
UI.durationBox = Box(page4, _G.HitboxDuration, 164)
UI.autoPunchBtn = Btn(page4, "ต่อยอัตโนมัติ " .. (_G.AutoPunchEnabled and "เปิด" or "ปิด"), 198)
UI.aimPunchBtn = Btn(page4, "เล็งตอนต่อย " .. (_G.AimPunchEnabled and "เปิด" or "ปิด"), 228)
UI.aimPunchModeBtn = Btn(page4, "โหมดเล็ง: " .. (AimPunch.Mode == "CamChar" and "กล้อง+ตัวละคร" or "ตัวละคร"), 258)

-- ========== หน้า 5: แทง ==========
local page5 = pages[5]
Lbl(page5, "แทงอัตโนมัติ", 6)
UI.autoStabBtn = Btn(page5, "แทงอัตโนมัติ " .. (stabEnabled and "เปิด" or "ปิด"), 26)
UI.stabModeBtn = Btn(page5, "โหมด: " .. (stabMode == "Behind" and "ด้านหลัง" or "รอบตัว"), 58)
Lbl(page5, "ระยะแทง:", 90)
UI.stabRangeBox = Box(page5, stabRange, 110)

-- ========== หน้า 6: บทบาท ==========
local page6 = pages[6]
Lbl(page6, "บทบาท / ความสามารถ", 4)
UI.voidrushBtn = Btn(page6, "ควบคุม Voidrush " .. (voidrushcontrol and "เปิด" or "ปิด"), 24)
UI.autoSlashBtn = Btn(page6, "สแลชอัตโนมัติ " .. (Slash.Enabled and "เปิด" or "ปิด"), 54)
Lbl(page6, "ระยะสแลช:", 84)
UI.slashRangeBox = Box(page6, Slash.Range, 104)
Lbl(page6, "ดีเลย์สแลช (0-1):", 134)
UI.slashDelayBox = Box(page6, Slash.Delay, 154)
UI.hitboxExtBtn = Btn(page6, "ขยายฮิตบ็อกซ์ " .. (HitboxExt.Enabled and "เปิด" or "ปิด"), 188)
Lbl(page6, "ขนาดฮิตบ็อกซ์:", 218)
UI.hitboxSizeBox = Box(page6, HitboxExt.Size, 238)

-- ========== หน้า 7: ป้องกัน ==========
local page7 = pages[7]
Lbl(page7, "ระบบป้องกัน", 4)
UI.antiStunBtn = Btn(page7, "ต้านสตัน " .. (antiStun and "เปิด" or "ปิด"), 24)
UI.antiSlowBtn = Btn(page7, "ต้านช้า " .. (antiSlow and "เปิด" or "ปิด"), 54)
UI.antiBlindBtn = Btn(page7, "ต้านตาบอด " .. (antiBlindness and "เปิด" or "ปิด"), 84)
UI.antiSubBtn = Btn(page7, "ต้านซับสเปซ " .. (antiSubspace and "เปิด" or "ปิด"), 114)
UI.antiHiddenBtn = Btn(page7, "แสดงสถิติที่ซ่อน " .. (toggleState and "เปิด" or "ปิด"), 144)

-- ========== หน้า 8: อันตราย ==========
local page8 = pages[8]
Lbl(page8, "โซนอันตราย", 6)
UI.toggleKeyBtn = Btn(page8, "ปุ่มเปิด/ปิด GUI: " .. Keys.Toggle.Name, 28)
UI.destroyBtn = Btn(page8, "ทำลาย GUI ทั้งหมด", 68)
UI.destroyBtn.TextColor3 = Color3.fromRGB(255, 80, 80)
UI.destroyBtn.BorderColor3 = Color3.fromRGB(255, 50, 50)

-- แถบด้านล่าง
local bottomBar = Instance.new("Frame")
bottomBar.Size = UDim2.new(1, 0, 0, 32)
bottomBar.Position = UDim2.new(0, 0, 1, -32)
bottomBar.BackgroundColor3 = BG
bottomBar.BorderColor3 = RED
bottomBar.BorderSizePixel = 2
bottomBar.BackgroundTransparency = 1
bottomBar.Parent = main

local bottomLabel = Instance.new("TextLabel")
bottomLabel.Size = UDim2.new(1, -16, 1, 0)
bottomLabel.Position = UDim2.new(0, 8, 0, 0)
bottomLabel.BackgroundTransparency = 1
bottomLabel.Text = "กดแท็บด้านบนเพื่อเปลี่ยนหมวด  |  ลากเพื่อย้ายหน้าต่าง"
bottomLabel.TextColor3 = Color3.fromRGB(160, 160, 160)
bottomLabel.Font = Enum.Font.SourceSans
bottomLabel.TextSize = 12
bottomLabel.TextXAlignment = Enum.TextXAlignment.Left
bottomLabel.TextTransparency = 1
bottomLabel.Parent = bottomBar

-- Fade in
task.delay(0.3, function()
	local info = TweenInfo.new(0.4, Enum.EasingStyle.Quad, Enum.EasingDirection.Out)
	TweenService:Create(main, info, { BackgroundTransparency = 0 }):Play()
	TweenService:Create(titleBar, info, { BackgroundTransparency = 0 }):Play()
	TweenService:Create(title, info, { TextTransparency = 0 }):Play()
	TweenService:Create(closeBtn, info, { BackgroundTransparency = 0, TextTransparency = 0 }):Play()
	TweenService:Create(tabBar, info, { BackgroundTransparency = 0 }):Play()
	TweenService:Create(content, info, { BackgroundTransparency = 0 }):Play()
	TweenService:Create(bottomBar, info, { BackgroundTransparency = 0 }):Play()
	TweenService:Create(bottomLabel, info, { TextTransparency = 0 }):Play()
	for _, tb in ipairs(tabButtons) do
		TweenService:Create(tb, info, { BackgroundTransparency = 0, TextTransparency = 0 }):Play()
	end
	for _, page in ipairs(pages) do
		for _, child in ipairs(page:GetDescendants()) do
			if child:IsA("TextLabel") then
				TweenService:Create(child, info, { TextTransparency = 0 }):Play()
			elseif child:IsA("TextButton") or child:IsA("TextBox") then
				TweenService:Create(child, info, { BackgroundTransparency = 0, TextTransparency = 0 }):Play()
			end
		end
	end
end)

-- Mini UI (มือถือ)
local mini = Instance.new("Frame")
mini.Size = UDim2.new(0, 160, 0, 28)
mini.Position = main.Position
mini.BackgroundColor3 = BG
mini.BorderColor3 = RED
mini.BorderSizePixel = 2
mini.Visible = false
mini.Active = true
mini.Parent = gui

local miniTitle = Instance.new("TextLabel")
miniTitle.Size = UDim2.new(1, -30, 1, 0)
miniTitle.Position = UDim2.new(0, 6, 0, 0)
miniTitle.BackgroundTransparency = 1
miniTitle.Text = "c00lSaken"
miniTitle.TextColor3 = TXT
miniTitle.Font = Enum.Font.SourceSansBold
miniTitle.TextSize = 14
miniTitle.TextXAlignment = Enum.TextXAlignment.Left
miniTitle.Parent = mini

local miniOpen = Instance.new("TextButton")
miniOpen.Size = UDim2.new(0, 26, 1, 0)
miniOpen.Position = UDim2.new(1, -26, 0, 0)
miniOpen.BackgroundColor3 = BG
miniOpen.BorderColor3 = RED
miniOpen.BorderSizePixel = 2
miniOpen.Text = "+"
miniOpen.TextColor3 = TXT
miniOpen.Font = Enum.Font.SourceSansBold
miniOpen.TextSize = 16
miniOpen.Parent = mini

-- Confirm destroy
local confirmFrame = Instance.new("Frame")
confirmFrame.Name = "ConfirmFrame"
confirmFrame.Size = UDim2.new(0, 260, 0, 140)
confirmFrame.Position = UDim2.new(0.5, -130, 0.5, -70)
confirmFrame.BackgroundColor3 = BG
confirmFrame.BorderColor3 = RED
confirmFrame.BorderSizePixel = 2
confirmFrame.Visible = false
confirmFrame.ZIndex = 10
confirmFrame.Active = true
confirmFrame.Parent = gui

local confirmLabel = Instance.new("TextLabel")
confirmLabel.Size = UDim2.new(1, -16, 0, 60)
confirmLabel.Position = UDim2.new(0, 8, 0, 10)
confirmLabel.BackgroundTransparency = 1
confirmLabel.Text = "แน่ใจหรือไม่ที่จะทำลาย GUI?\nฟีเจอร์ทั้งหมดจะถูกปิดทันที!"
confirmLabel.TextColor3 = TXT
confirmLabel.Font = Enum.Font.SourceSansBold
confirmLabel.TextSize = 14
confirmLabel.TextWrapped = true
confirmLabel.ZIndex = 11
confirmLabel.Parent = confirmFrame

local confirmYes = Instance.new("TextButton")
confirmYes.Size = UDim2.new(0.45, -6, 0, 30)
confirmYes.Position = UDim2.new(0, 8, 1, -42)
confirmYes.BackgroundColor3 = BG
confirmYes.BorderColor3 = RED
confirmYes.BorderSizePixel = 2
confirmYes.Text = "ใช่"
confirmYes.TextColor3 = Color3.fromRGB(255, 80, 80)
confirmYes.Font = Enum.Font.SourceSansBold
confirmYes.TextSize = 14
confirmYes.ZIndex = 11
confirmYes.Parent = confirmFrame

local confirmCancel = Instance.new("TextButton")
confirmCancel.Size = UDim2.new(0.45, -6, 0, 30)
confirmCancel.Position = UDim2.new(0.55, 0, 1, -42)
confirmCancel.BackgroundColor3 = BG
confirmCancel.BorderColor3 = RED
confirmCancel.BorderSizePixel = 2
confirmCancel.Text = "ยกเลิก"
confirmCancel.TextColor3 = TXT
confirmCancel.Font = Enum.Font.SourceSansBold
confirmCancel.TextSize = 14
confirmCancel.ZIndex = 11
confirmCancel.Parent = confirmFrame

local function CleanUpAllFeatures()
	infEnabled = false
	stamina.StaminaLossDisabled = false
	sprintSpeedEnabled = false
	applySprintSpeed()
	
	for k in pairs(ESPSettings) do
		if type(ESPSettings[k]) == "boolean" then ESPSettings[k] = false end
	end
	pcall(UpdateESP)
	
	_G.AutoBlockEnabled = false
	DisableAutoBlock()
	
	_G.AutoPunchEnabled = false
	_G.AimPunchEnabled = false
	toggleModule("Punch", false)
	
	stabEnabled = false
	if stabConnection then stabConnection:Disconnect() end

	Slash.Enabled = false
	Slash.Busy = false
	if Slash.Connection then Slash.Connection:Disconnect() Slash.Connection = nil end

	if HitboxExt.Enabled then
		setHitboxExt(false)
	end
	HitboxExt.Enabled = false
	
	voidrushcontrol = false
	stopOverride()
	
	autoGen = false
	antiStun = false
	antiSlow = false
	antiBlindness = false
	antiSubspace = false
	antiHiddenStats = false
	toggleState = false
	pcall(function() hiddenStatsFunc(false) end)
	espLoopRunning = false
	autoPunchRunning = false
	autoBlockMonitor = false
end

confirmYes.MouseButton1Click:Connect(function()
	CleanUpAllFeatures()
	gui:Destroy()
end)

confirmCancel.MouseButton1Click:Connect(function()
	confirmFrame.Visible = false
end)

UI.destroyBtn.MouseButton1Click:Connect(function()
	confirmFrame.Visible = true
end)

local isMobile = UIS.TouchEnabled and not UIS.KeyboardEnabled

local function ToggleGUI_PC()
	hidden = not hidden
	main.Visible = not hidden
	mini.Visible = false
	if hidden then
		pcall(function()
			StarterGui:SetCore("SendNotification", {
				Title = "c00lSaken",
				Text = "กด [ " .. Keys.Toggle.Name .. " ] เพื่อเปิด GUI อีกครั้ง",
				Duration = 4
			})
		end)
	end
end

local function ToggleGUI_Mobile()
	hidden = not hidden
	main.Visible = not hidden
	mini.Visible = hidden
	if hidden then 
		mini.Position = main.Position 
	else 
		main.Position = mini.Position 
	end
end

local function OnCloseClicked()
	if isMobile then
		ToggleGUI_Mobile()
	else
		ToggleGUI_PC()
	end
end

closeBtn.MouseButton1Click:Connect(OnCloseClicked)
miniOpen.MouseButton1Click:Connect(ToggleGUI_Mobile)

local function onCharacterAdded(newChar)
	local hum = newChar:WaitForChild("Humanoid", 10)
	local hrp = newChar:WaitForChild("HumanoidRootPart", 10)
	if not hum or not hrp then return end

	task.defer(function()
		if infEnabled then
			stamina.StaminaLossDisabled = true
			stamina.Stamina = stamina.MaxStamina
		end
		applySprintSpeed()
	end)

	if _G.AutoBlockEnabled then
		task.defer(function()
			task.wait(0.8)
			DisableAutoBlock()
			GetObjects()
			EnableAutoBlock()
		end)
	end

	if _G.AimPunchEnabled then
		toggleModule("Punch", false)
		task.wait(0.1)
		toggleModule("Punch", true)
	end

	if voidrushcontrol then
		stopOverride()
	end
end

if LP.Character then
	task.spawn(function() onCharacterAdded(LP.Character) end)
end
LP.CharacterAdded:Connect(onCharacterAdded)

task.spawn(function()
	while task.wait(0.1) do
		if infEnabled then
			stamina.Stamina = stamina.MaxStamina
			stamina.StaminaLossDisabled = true
		end
	end
end)

-- ========== Event ปุ่มทั้งหมด ==========
UI.killerESPBtn.MouseButton1Click:Connect(function()
	ESPSettings.killerESP = not ESPSettings.killerESP
	UI.killerESPBtn.Text = "ESP ฆาตกร " .. (ESPSettings.killerESP and "เปิด" or "ปิด")
	SaveConfig()
end)

UI.survivorESPBtn.MouseButton1Click:Connect(function()
	ESPSettings.playerESP = not ESPSettings.playerESP
	UI.survivorESPBtn.Text = "ESP ผู้รอด " .. (ESPSettings.playerESP and "เปิด" or "ปิด")
	SaveConfig()
end)

UI.genESPBtn.MouseButton1Click:Connect(function()
	ESPSettings.generatorESP = not ESPSettings.generatorESP
	UI.genESPBtn.Text = "ESP เครื่องปั่น " .. (ESPSettings.generatorESP and "เปิด" or "ปิด")
	SaveConfig()
end)

UI.itemESPBtn.MouseButton1Click:Connect(function()
	ESPSettings.itemESP = not ESPSettings.itemESP
	UI.itemESPBtn.Text = "ESP ไอเทม " .. (ESPSettings.itemESP and "เปิด" or "ปิด")
	SaveConfig()
end)

UI.pizzaESPBtn.MouseButton1Click:Connect(function()
	ESPSettings.pizzaEsp = not ESPSettings.pizzaEsp
	UI.pizzaESPBtn.Text = "ESP พิซซ่า " .. (ESPSettings.pizzaEsp and "เปิด" or "ปิด")
	SaveConfig()
end)

UI.deliveryESPBtn.MouseButton1Click:Connect(function()
	ESPSettings.pizzaDeliveryEsp = not ESPSettings.pizzaDeliveryEsp
	UI.deliveryESPBtn.Text = "ESP ส่งพิซซ่า " .. (ESPSettings.pizzaDeliveryEsp and "เปิด" or "ปิด")
	SaveConfig()
end)

UI.zombieESPBtn.MouseButton1Click:Connect(function()
	ESPSettings.zombieEsp = not ESPSettings.zombieEsp
	UI.zombieESPBtn.Text = "ESP ซอมบี้ " .. (ESPSettings.zombieEsp and "เปิด" or "ปิด")
	SaveConfig()
end)

UI.tripwireESPBtn.MouseButton1Click:Connect(function()
	ESPSettings.taphTripwireEsp = not ESPSettings.taphTripwireEsp
	UI.tripwireESPBtn.Text = "ESP สายดัก " .. (ESPSettings.taphTripwireEsp and "เปิด" or "ปิด")
	SaveConfig()
end)

UI.mineESPBtn.MouseButton1Click:Connect(function()
	ESPSettings.tripMineEsp = not ESPSettings.tripMineEsp
	UI.mineESPBtn.Text = "ESP ระเบิด " .. (ESPSettings.tripMineEsp and "เปิด" or "ปิด")
	SaveConfig()
end)

UI.respawnESPBtn.MouseButton1Click:Connect(function()
	ESPSettings.twoTimeRespawnEsp = not ESPSettings.twoTimeRespawnEsp
	UI.respawnESPBtn.Text = "ESP จุดเกิด " .. (ESPSettings.twoTimeRespawnEsp and "เปิด" or "ปิด")
	SaveConfig()
end)

UI.graffitiESPBtn.MouseButton1Click:Connect(function()
	ESPSettings.graffitiEsp = not ESPSettings.graffitiEsp
	UI.graffitiESPBtn.Text = "ESP กราฟฟิตี้ " .. (ESPSettings.graffitiEsp and "เปิด" or "ปิด")
	SaveConfig()
end)

UI.groundBulbESPBtn.MouseButton1Click:Connect(function()
	ESPSettings.azureGroundBulbESP = not ESPSettings.azureGroundBulbESP
	UI.groundBulbESPBtn.Text = "ESP หัวหลอด " .. (ESPSettings.azureGroundBulbESP and "เปิด" or "ปิด")
	SaveConfig()
end)

UI.vineESPBtn.MouseButton1Click:Connect(function()
	ESPSettings.azureVineESP = not ESPSettings.azureVineESP
	UI.vineESPBtn.Text = "ESP เถาวัลย์ " .. (ESPSettings.azureVineESP and "เปิด" or "ปิด")
	SaveConfig()
end)

UI.antiStunBtn.MouseButton1Click:Connect(function()
	antiStun = not antiStun
	UI.antiStunBtn.Text = "ต้านสตัน " .. (antiStun and "เปิด" or "ปิด")
	SaveConfig()
end)

UI.antiSlowBtn.MouseButton1Click:Connect(function()
	antiSlow = not antiSlow
	UI.antiSlowBtn.Text = "ต้านช้า " .. (antiSlow and "เปิด" or "ปิด")
	SaveConfig()
end)

UI.antiBlindBtn.MouseButton1Click:Connect(function()
	antiBlindness = not antiBlindness
	UI.antiBlindBtn.Text = "ต้านตาบอด " .. (antiBlindness and "เปิด" or "ปิด")
	SaveConfig()
end)

UI.antiSubBtn.MouseButton1Click:Connect(function()
	antiSubspace = not antiSubspace
	UI.antiSubBtn.Text = "ต้านซับสเปซ " .. (antiSubspace and "เปิด" or "ปิด")
	SaveConfig()
end)

UI.antiHiddenBtn.MouseButton1Click:Connect(function()
	toggleState = not toggleState
	antiHiddenStats = toggleState
	hiddenStatsFunc(toggleState)
	UI.antiHiddenBtn.Text = "แสดงสถิติที่ซ่อน " .. (toggleState and "เปิด" or "ปิด")
	SaveConfig()
end)

local function ToggleHakariDance(state)
	local char = LP.Character
	if not char then return end
	local humanoid = char:FindFirstChildOfClass("Humanoid")
	local rootPart = char:FindFirstChild("HumanoidRootPart")
	if not humanoid or not rootPart then return end
	if state then
		hakariActive = true
		humanoid.PlatformStand = true
		humanoid.JumpPower = 0
		local bv = Instance.new("BodyVelocity")
		bv.Name = "HakariBV"
		bv.MaxForce = Vector3.new(1e5,1e5,1e5)
		bv.Velocity = Vector3.zero
		bv.Parent = rootPart
		local anim = Instance.new("Animation")
		anim.AnimationId = "rbxassetid://138019937280193"
		local track = humanoid:LoadAnimation(anim)
		track:Play()
		local sound = Instance.new("Sound")
		sound.Name = "HakariSound"
		sound.SoundId = "rbxassetid://87166578676888"
		sound.Volume = 0.5
		sound.Looped = true
		sound.Parent = rootPart
		sound:Play()
		pcall(function()
			local effect = ReplicatedStorage.Assets.Emotes.HakariDance.HakariBeamEffect:Clone()
			effect.Name = "PlayerEmoteVFX"
			effect.CFrame = char.PrimaryPart.CFrame * CFrame.new(0,-1,-0.3)
			effect.WeldConstraint.Part0 = char.PrimaryPart
			effect.WeldConstraint.Part1 = effect
			effect.Parent = char
			effect.CanCollide = false
		end)
		pcall(function()
			ReplicatedStorage.Modules.Network.RemoteEvent:FireServer("PlayEmote", "Animations", "HakariDance")
		end)
		track.Stopped:Connect(function()
			if not hakariActive then
				humanoid.PlatformStand = false
				local b = rootPart:FindFirstChild("HakariBV")
				if b then b:Destroy() end
			end
		end)
	else
		hakariActive = false
		humanoid.PlatformStand = false
		humanoid.JumpPower = 0
		local b = rootPart:FindFirstChild("HakariBV")
		if b then b:Destroy() end
		local s = rootPart:FindFirstChild("HakariSound")
		if s then s:Stop() s:Destroy() end
		local e = char:FindFirstChild("PlayerEmoteVFX")
		if e then e:Destroy() end
		for _, t in ipairs(humanoid:GetPlayingAnimationTracks()) do
			if t.Animation.AnimationId == "rbxassetid://138019937280193" then t:Stop() end
		end
	end
end

UI.toggleKeyBtn.MouseButton1Click:Connect(function()
	WaitingKey.Toggle = true
	UI.toggleKeyBtn.Text = "กดปุ่มใดก็ได้..."
end)

UIS.InputBegan:Connect(function(input, gp)
	if gp then return end
	if WaitingKey.Hakari then
		if input.UserInputType == Enum.UserInputType.Keyboard then
			Keys.Hakari = input.KeyCode
			UI.keyButton.Text = "ปุ่มฮาคาริ: " .. input.KeyCode.Name
			WaitingKey.Hakari = false
			SaveConfig()
		end
		return
	end
	if WaitingKey.Solve then
		if input.UserInputType == Enum.UserInputType.Keyboard then
			Keys.Solve = input.KeyCode
			UI.solveKeyBtn.Text = "ปุ่มแก้ปริศนา: " .. input.KeyCode.Name
			WaitingKey.Solve = false
			SaveConfig()
		end
		return
	end
	if WaitingKey.Toggle then
		if input.UserInputType == Enum.UserInputType.Keyboard then
			Keys.Toggle = input.KeyCode
			UI.toggleKeyBtn.Text = "ปุ่มเปิด/ปิด GUI: " .. input.KeyCode.Name
			WaitingKey.Toggle = false
			SaveConfig()
		end
		return
	end
	if input.KeyCode == Keys.Toggle then 
		if isMobile then
			ToggleGUI_Mobile()
		else
			ToggleGUI_PC()
		end
	elseif input.KeyCode == Keys.Hakari then ToggleHakariDance(not hakariActive)
	elseif input.KeyCode == Keys.Solve then SolveNow() end
end)

local function MakeDrag(frame)
	local drag, start, pos
	frame.InputBegan:Connect(function(i)
		if i.UserInputType == Enum.UserInputType.MouseButton1 or i.UserInputType == Enum.UserInputType.Touch then
			drag = true start = i.Position pos = frame.Position
			i.Changed:Connect(function()
				if i.UserInputState == Enum.UserInputState.End then drag = false end
			end)
		end
	end)
	UIS.InputChanged:Connect(function(i)
		if drag and (i.UserInputType == Enum.UserInputType.MouseMovement or i.UserInputType == Enum.UserInputType.Touch) then
			local d = i.Position - start
			frame.Position = UDim2.new(pos.X.Scale, pos.X.Offset + d.X, pos.Y.Scale, pos.Y.Offset + d.Y)
		end
	end)
end
MakeDrag(main)
MakeDrag(mini)
MakeDrag(confirmFrame)

UI.infButton.MouseButton1Click:Connect(function()
	infEnabled = not infEnabled
	stamina.StaminaLossDisabled = infEnabled
	UI.infButton.Text = "สตามินาไม่จำกัด " .. (infEnabled and "เปิด" or "ปิด")
	SaveConfig()
end)

UI.sprintSpeedBtn.MouseButton1Click:Connect(function()
	sprintSpeedEnabled = not sprintSpeedEnabled
	applySprintSpeed()
	UI.sprintSpeedBtn.Text = "ความเร็ววิ่ง " .. (sprintSpeedEnabled and "เปิด" or "ปิด")
	SaveConfig()
end)

UI.speedBox.FocusLost:Connect(function()
	local n = tonumber(UI.speedBox.Text)
	if n and n > 0 then
		sprintSpeedValue = n
		applySprintSpeed()
		SaveConfig()
	else
		UI.speedBox.Text = tostring(sprintSpeedValue)
	end
end)

UI.keyButton.MouseButton1Click:Connect(function() WaitingKey.Hakari = true UI.keyButton.Text = "กดปุ่มใดก็ได้..." end)
UI.solveButton.MouseButton1Click:Connect(SolveNow)
UI.solveKeyBtn.MouseButton1Click:Connect(function() WaitingKey.Solve = true UI.solveKeyBtn.Text = "กดปุ่มใดก็ได้..." end)

UI.voidrushBtn.MouseButton1Click:Connect(function()
	voidrushcontrol = not voidrushcontrol
	if not voidrushcontrol then stopOverride() end
	UI.voidrushBtn.Text = "ควบคุม Voidrush " .. (voidrushcontrol and "เปิด" or "ปิด")
	SaveConfig()
end)

UI.autoGenBtn.MouseButton1Click:Connect(function()
	autoGen = not autoGen
	UI.autoGenBtn.Text = "ออโต้เครื่องปั่น " .. (autoGen and "เปิด" or "ปิด")
	SaveConfig()
end)

UI.genSpeedBox.FocusLost:Connect(function()
	local n = tonumber(UI.genSpeedBox.Text)
	if n and n >= 0.01 and n <= 1 then
		autoGenSpeed = n
		SaveConfig()
	else
		UI.genSpeedBox.Text = tostring(autoGenSpeed)
	end
end)

UI.autoBlockBtn.MouseButton1Click:Connect(function()
	_G.AutoBlockEnabled = not _G.AutoBlockEnabled
	UI.autoBlockBtn.Text = "บล็อกอัตโนมัติ " .. (_G.AutoBlockEnabled and "เปิด" or "ปิด")
	if _G.AutoBlockEnabled then EnableAutoBlock() else DisableAutoBlock() end
	SaveConfig()
end)

UI.showHitboxBtn.MouseButton1Click:Connect(function()
	_G.ShowHitboxVisual = not _G.ShowHitboxVisual
	UI.showHitboxBtn.Text = "แสดงฮิตบ็อกซ์ " .. (_G.ShowHitboxVisual and "เปิด" or "ปิด")
	SaveConfig()
end)

UI.scaleBox.FocusLost:Connect(function()
	local n = tonumber(UI.scaleBox.Text)
	if n and n > 0 then _G.HitboxScale = n SaveConfig() else UI.scaleBox.Text = tostring(_G.HitboxScale) end
end)

UI.offsetBox.FocusLost:Connect(function()
	local n = tonumber(UI.offsetBox.Text)
	if n then _G.HitboxOffset = n SaveConfig() else UI.offsetBox.Text = tostring(_G.HitboxOffset) end
end)

UI.durationBox.FocusLost:Connect(function()
	local n = tonumber(UI.durationBox.Text)
	if n and n > 0 then _G.HitboxDuration = n SaveConfig() else UI.durationBox.Text = tostring(_G.HitboxDuration) end
end)

UI.autoPunchBtn.MouseButton1Click:Connect(function()
	_G.AutoPunchEnabled = not _G.AutoPunchEnabled
	UI.autoPunchBtn.Text = "ต่อยอัตโนมัติ " .. (_G.AutoPunchEnabled and "เปิด" or "ปิด")
	SaveConfig()
end)

UI.aimPunchBtn.MouseButton1Click:Connect(function()
	_G.AimPunchEnabled = not _G.AimPunchEnabled
	UI.aimPunchBtn.Text = "เล็งตอนต่อย " .. (_G.AimPunchEnabled and "เปิด" or "ปิด")
	toggleModule("Punch", _G.AimPunchEnabled)
	SaveConfig()
end)

UI.aimPunchModeBtn.MouseButton1Click:Connect(function()
	AimPunch.Mode = (AimPunch.Mode == "CamChar") and "Character" or "CamChar"
	UI.aimPunchModeBtn.Text = "โหมดเล็ง: " .. (AimPunch.Mode == "CamChar" and "กล้อง+ตัวละคร" or "ตัวละคร")
	SaveConfig()
end)

UI.autoStabBtn.MouseButton1Click:Connect(function()
	stabEnabled = not stabEnabled
	UI.autoStabBtn.Text = "แทงอัตโนมัติ " .. (stabEnabled and "เปิด" or "ปิด")
	SaveConfig()
end)

UI.stabModeBtn.MouseButton1Click:Connect(function()
	stabMode = (stabMode == "Behind" and "Around" or "Behind")
	UI.stabModeBtn.Text = "โหมด: " .. (stabMode == "Behind" and "ด้านหลัง" or "รอบตัว")
	SaveConfig()
end)

UI.stabRangeBox.FocusLost:Connect(function()
	local n = tonumber(UI.stabRangeBox.Text)
	if n and n > 0 then stabRange = n SaveConfig() else UI.stabRangeBox.Text = tostring(stabRange) end
end)

UI.autoSlashBtn.MouseButton1Click:Connect(function()
	Slash.Enabled = not Slash.Enabled
	UI.autoSlashBtn.Text = "สแลชอัตโนมัติ " .. (Slash.Enabled and "เปิด" or "ปิด")
	SaveConfig()
end)

UI.slashRangeBox.FocusLost:Connect(function()
	local n = tonumber(UI.slashRangeBox.Text)
	if n and n > 0 then Slash.Range = n SaveConfig() else UI.slashRangeBox.Text = tostring(Slash.Range) end
end)

UI.slashDelayBox.FocusLost:Connect(function()
	local n = tonumber(UI.slashDelayBox.Text)
	if n and n >= 0 and n <= 1 then
		Slash.Delay = n
		SaveConfig()
	else
		UI.slashDelayBox.Text = tostring(Slash.Delay)
	end
end)

UI.hitboxExtBtn.MouseButton1Click:Connect(function()
	setHitboxExt(not HitboxExt.Enabled)
	UI.hitboxExtBtn.Text = "ขยายฮิตบ็อกซ์ " .. (HitboxExt.Enabled and "เปิด" or "ปิด")
	SaveConfig()
end)

UI.hitboxSizeBox.FocusLost:Connect(function()
	local n = tonumber(UI.hitboxSizeBox.Text)
	if n and n > 0 then
		HitboxExt.Size = n
		SaveConfig()
		if HitboxExt.Enabled then
			setHitboxExt(false)
			task.wait(0.05)
			setHitboxExt(true)
			UI.hitboxExtBtn.Text = "ขยายฮิตบ็อกซ์ เปิด"
		end
	else
		UI.hitboxSizeBox.Text = tostring(HitboxExt.Size)
	end
end)

if _G.AutoBlockEnabled then task.defer(EnableAutoBlock) end
if HitboxExt.Enabled then
	task.defer(function()
		task.wait(0.5)
		setHitboxExt(true)
	end)
end
if sprintSpeedEnabled then
	task.defer(applySprintSpeed)
end
